# Sidebar visual asset

The approved Venezia hotel image is stored as `sidebar-hotel.jpg`.
The dashboard sidebar renders it with `background-size: cover`, `background-position: center bottom`, and a navy overlay.
